name: Trunk Code Quality
on:
  workflow_dispatch:
  pull_request:
    branches: main

permissions:
  contents: read

jobs:
  trunk-code-quality:
    uses: hypermodeinc/.github/.github/workflows/trunk.yml@main
